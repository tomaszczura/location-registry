<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="8dp">

    <RadioButton
        android:id="@+id/isActiveBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/userName"
        android:layout_width="0dp"
        style="@style/TextView.Subtitle1"
        android:layout_marginStart="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@android:color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/editUserBtn"
        app:layout_constraintStart_toEndOf="@+id/isActiveBtn"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="John Smith" />

    <ImageButton
        android:id="@+id/editUserBtn"
        style="@style/ListRowButton"
        android:src="@drawable/ic_edit"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toStartOf="@+id/removeUserBtn"/>

    <ImageButton
        android:id="@+id/removeUserBtn"
        style="@style/ListRowButton"
        android:src="@drawable/ic_delete"
        app:layout_constraintEnd_toEndOf="parent"/>


</android.support.constraint.ConstraintLayout>